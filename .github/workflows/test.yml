name: Test

on:
  push:
    branches:
      - main
      - test-branch-002

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Tests
        run: echo "Running tests..."
